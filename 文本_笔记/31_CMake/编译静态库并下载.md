# 简单示例

```cmake
add_library(${PROJECT_NAME} STATIC source.cpp include.h)  # 编译生成静态库文件
target_link_libraries(${PROJECT_NAME} PUBLIC ...） # 链接依赖库
target_include_directories(${PROJECT_NAME} PRIVATE ...）# 指定编译时(生成静态库文件), 查找头文件的路径
```

# 命令

## install

```cmake
install(TARGETS ${PROJECT_NAME} # 要安装的目标
	 CONFIGURATIONS ${CMAKE_BUILD_TYPE} # 要安装的构建类类型（debug 或 release 等）
	 EXPORT ${PROJECT_NAME}-targets  # 导出
	 PUBLIC_HEADER DESTINATION include/${PROJECT_NAME}  # 指定要安装库的头文件的目录
     ARCHIVE DESTINATION lib/${CMAKE_BUILD_TYPE} # 静态库 .a
     LIBRARY DESTINATION lib/${CMAKE_BUILD_TYPE} # 动态库 .so
     RUNTIME DESTINATION bin/${CMAKE_BUILD_TYPE}) # 可执行文件 
)
# 包含了库的一些元信息，比如库的名称、版本、目录结构等
# 会在指定的目录下创建一个 CMake 模块文件，文件名为 ${PROJECT_NAME}Config.cmake
# 该文件中会定义一个名为 ${PROJECT_NAME}-targets 的 CMake 导出集，其中包含了该库的导出目标。
install(EXPORT ${PROJECT_NAME}-targets 
    NAMESPACE KdrobotCppLibs::  # 会在该文件中定义一个 KdrobotCppLibs:: 命名空间
    FILE ${PROJECT_NAME}Config.cmake # 
    DESTINATION lib/cmake/${PROJECT_NAME} # 指定目录
)
```

---

关于 `EXPORT`

在 CMake 中使用 `export` 是将构建目标安装的一种方式，通过它可以将库的构建信息导出到外部使用。当你的项目是库的时候，通常你需要将库的头文件、库文件和 CMake 配置文件安装到系统路径中以便其他程序使用。使用 `export` 可以将库的构建信息保存到一份 CMake 脚本中，这样在其他项目中，只需要使用 `find_package` 指定这份 CMake 脚本，就可以直接使用库，而不用手动设置库的包含路径、库路径和链接库等信息。

如果你不使用 `export`，而是手动在项目中设置库的路径和链接库等信息，这样虽然也能使用库，但是会显得比较麻烦，而且如果库的版本有更新，还需要手动修改库的路径等信息。

以上引用自 `ChatGPT`

总之就是 用`export` 你别的程序用的时候直接find_package()；如果不用还要配置库的路径和链接库等信息